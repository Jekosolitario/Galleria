/* const filtri = document.querySelectorAll('button[data-categoria]');

filtri.forEach(bottone => {
  bottone.addEventListener('click', () => {
    const categoria = bottone.dataset.categoria; // <-- ecco dove lo leggi
    console.log("Hai cliccato il filtro:", categoria);

    // qui puoi far apparire solo le immagini con quella categoria
  });
});
 */